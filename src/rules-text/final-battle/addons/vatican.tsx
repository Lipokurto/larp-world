import { Item } from '../../type';

export const vatican: Item = {
  label: 'Поддержка Ватикана',
  element:
    <div>Представители людей получают ленты воскрешения (количество определяется отдельно).</div>
}