import { Item } from '../../type';

export const lastStand: Item = {
  label: 'Последний поход',
  element:
    <div>Инквизиция может набрать в свои ряды добровольцев, но не выше определенного лимита.</div>
}